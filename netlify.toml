[build]
base = "public-frontend"
command = "npm run build"
publish = ".next"

[build.environment]
NODE_VERSION = "20"

# Configure these in the Netlify dashboard for a real deployment:
# NEXT_PUBLIC_API_URL=https://your-backend.example.com/api/v1
# INTERNAL_API_URL=https://your-backend.example.com/api/v1
